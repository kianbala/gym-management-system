🏋️‍♂️ سامانه مدیریت هوشمند باشگاه و پیش‌بینی ریزش اعضا
یک نرم‌افزار کاربردی جهت مدیریت ثبت‌نام، تمدید اشتراک، کنترل تردد و تحلیل هوشمند داده‌های باشگاه با استفاده از Python، Streamlit و SQL Server.

🌟 ویژگی‌های کلیدی پروژه

📋 مدیریت اعضا: ثبت‌نام با بررسی اعتبار کد ملی و شماره تماس، جستجوی پیشرفته و مدیریت وضعیت اشتراک‌ها.

💳 مدیریت بسته‌ها و اشتراک‌ها: تعریف بسته‌های مختلف ورزشی و امکان تمدید یا تخصیص بسته جدید.

🚪 کنترل تردد (Check-in): ثبت ورود ورزشکاران و کسر خودکار جلسات از اشتراک فعال.

🤖 ماژول تحلیل و هوش مصنوعی:

تحلیل ساعات شلوغی: استخراج و نمایش نموداری پراکندگی ورود ورزشکاران در ۲۴ ساعت شبانه‌روز.

پیش‌بینی ریسک ریزش (Churn Risk): محاسبه امتیاز عددی ریسک عدم بازگشت اعضا (۰ تا ۱۰۰) بر اساس غیبت و جلسات باقی‌مانده.

📊 گزارش‌گیری ساختاریافته: استفاده از Viewهای تحلیلی دیتابیس برای گزارش‌گیری سریع.

🛠️ ابزارهای مدیریتی: پاکسازی و مدیریت اشتراک‌های منقضی‌شده و قابلیت ریست کامل دیتابیس با تایید چندمرحله‌ای.

🛠️ تکنولوژی‌های استفاده‌شده

Language: Python

Frontend / UI: Streamlit

Database: Microsoft SQL Server

Libraries: Pandas, PyODBC, NumPy, Matplotlib

🚀 راهنمای نصب و اجرای پروژه

۱. پیش‌نیازها

ابتدا پایتون و SQL Server را روی سیستم خود نصب کنید. سپس اسکریپت ساخت جداول دیتابیس (create_table.sql) را در SQL Server اجرا کنید تا پایگاه داده GymManagementDB ساخته شود.

۲. نصب کتابخانه‌ها

دستور زیر را در ترمینال اجرا کنید تا تمامی وابستگی‌ها نصب شوند:

pip install -r requirements.txt

۳. تنظیمات اتصال دیتابیس

در فایل db_manager.py متغیرهای مربوط به اتصال را مطابق مشخصات سرور خود مقداردهی کنید:

SERVER_NAME = r'YOUR_SERVER_NAME'

DATABASE_NAME = 'GymManagementDB'

USERNAME = 'YOUR_SQL_USERNAME'

PASSWORD = 'YOUR_SQL_PASSWORD'


۴. تولید داده‌های نمونه (اختیاری)
برای پر کردن دیتابیس با داده‌های آزمایشی جهت تست و ارزیابی ماژول هوش مصنوعی، دستور زیر را در ترمینال اجرا کنید:

python generate_dummy_data.py

۵. اجرای برنامه
با دستور زیر در ترمینال، برنامه Streamlit را اجرا کنید:

streamlit run app.py

🧠 ساختار و منطق محاسباتی ماژول پیش‌بینی ریزش اعضا (Churn Risk AI)
در این سامانه، به‌منظور شناسایی پیش‌دستانه اعضایی که در معرض انصراف یا عدم تمدید اشتراک قرار دارند، یک الگوریتم ارزیابی ریسک مبتنی بر قوانین و وزن‌دهی شاخص‌های کلیدی (Rule-based Scoring System) طراحی و پیاده‌سازی شده است.

📊 شاخص‌ها و فرمول ریاضی محاسبه امتیاز ریسک (محدوده ۰ تا ۱۰۰):

۱. تحلیل شاخص عدم حضور (Absence Days - وزن حداکثر ۷۰ امتیاز):

تعداد روزهای سپری‌شده از آخرین تردد ثبت‌شده برای هر عضو محاسبه می‌گردد.

به ازای هر روز غیبت، ۵ امتیاز ریسک لحاظ می‌شود (حداکثر سقف این شاخص ۷۰ امتیاز، معادل ۱۴ روز عدم حضور متوالی است).

در صورت عدم ثبت هرگونه سابقه تردد برای عضو، مقدار پیش‌فرض ۳۰ روز غیبت مبنای محاسبه قرار می‌گیرد.

۲. تحلیل اعتبار و جلسات باقیمانده (Remaining Sessions - وزن حداکثر ۳۰ امتیاز):

عدم وجود جلسه باقیمانده (۰ جلسه): تخصیص ۳۰ امتیاز ریسک (نشان‌دهنده اتمام اعتبار و لزوم پیگیری جهت تمدید).

جلسات باقیمانده ۳ یا کمتر: تخصیص ۱۵ امتیاز ریسک.

جلسات باقیمانده بیش از ۳: تخصیص ۰ امتیاز ریسک.

📈 رده‌بندی سطوح ریسک (Risk Classification):

🔴 ریسک بالا (High Risk - امتیاز ۷۰ و بالاتر): نیازمند پیگیری فوری و اقدامات بازدارنده جهت جلوگیری از ریزش.

🟡 ریسک متوسط (Medium Risk - امتیاز ۴۰ تا ۶۹): نیازمند ارائه مشوق‌ها یا تخفیف‌های دوره‌ای جهت ترغیب به تمدید.

🟢 ریسک پایین (Low Risk - امتیاز کمتر از ۴۰): وضعیت پایدار و عضویت فعال.
